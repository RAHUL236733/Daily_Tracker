VITE_API_URL=http://localhost:5000
# In production, set VITE_API_URL to your deployed backend URL, for example https://api.example.com
# Use HTTPS backend URL in production to support secure cookie auth.